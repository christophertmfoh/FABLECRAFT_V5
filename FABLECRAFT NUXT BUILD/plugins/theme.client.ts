// Client-side plugin for theme initialization
export default defineNuxtPlugin(() => {
  // The theme initialization is already handled in the useTheme composable
  // This plugin is kept for potential future enhancements
  
  // Note: System preference watching is already handled in useTheme composable
  // to avoid duplicate event listeners and potential conflicts
})
